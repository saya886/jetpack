# 接口规则

接口契约以 `server/openapi/openapi.yaml` 为准。

## 变更要求

- 后端新增、删除或修改接口时，必须同步更新 OpenAPI。
- 客户端应通过 `client/api-client` 封装接口调用。
- 影响客户端的接口变化必须在 PR 中说明。
- 字段含义不明确时，不直接猜测，应补充功能文档或在 Issue 中确认。

## 当前基础接口

- `GET /health`：服务健康检查。
- `GET /api/v1/status`：项目基础状态，用于客户端联通验证。

